#include <reg51.h>
#include <stdio.h>

void main(void)
{
    SCON = 0x50;    
    
    TMOD = 0x20;
    
    TH1 = 0xFD;     
    TR1 = 1;        /* Start Timer 1 */
    
    TI = 1;         /* Set TI to enable first transmission */

    while (1)
    {
        printf(" JAYANTHINATHAN!\r\n ");
    }
}
